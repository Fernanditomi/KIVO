# Puerto del backend
PORT=3000

# Conexion a PostgreSQL (host = nombre del servicio en docker-compose)
DATABASE_URL=postgres://kivo:kivo123@db:5432/kivo

# Secreto para firmar los tokens JWT (cambialo por uno largo y aleatorio)
JWT_SECRET=cambia-esto-por-un-secreto-muy-largo-aleatorio

# OneSignal (opcional hasta conectar la App)
ONESIGNAL_APP_ID=
ONESIGNAL_REST_API_KEY=